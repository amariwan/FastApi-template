[project]
name = "fastapi-cookiecutter-template"
version = "0.1.0"
description = "Maintenance tooling and smoke tests for the FastAPI Cookiecutter template."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "cookiecutter>=2.6.0",
    "pytest>=8.3.0",
]
